name: Build TCP voice chat

on:
  push:
    branches: ['tcp/26.*']
  pull_request:
    branches: ['tcp/26.*']
  workflow_dispatch:

permissions:
  contents: read

jobs:
  transport:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '25'
      - name: Run real TCP transport regressions
        run: bash scripts/test-tcp.sh
      - name: Run central proxy protocol regressions
        run: bash scripts/test-proxy.sh
      - name: Run central proxy session and backend regressions
        run: bash scripts/test-runtime.sh
      - name: Run actual client lifecycle regressions with game and audio doubles
        run: python3 tests/client/run.py

  build:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '25'
      - uses: gradle/actions/setup-gradle@v6
      - name: Build supported platforms
        shell: bash
        run: |
          tasks=(:fabric:build :paper:build :neoforge:build)
          if ! grep -q '^minecraft_version=26.3' gradle.properties; then
            tasks+=(:forge:build)
          else
            tasks+=(:bukkit:build :velocity:build :bungeecord:build)
          fi
          bash gradlew "${tasks[@]}" --console=plain
      - uses: actions/upload-artifact@v4
        with:
          name: simple-voice-chat-tcp
          if-no-files-found: error
          path: |
            fabric/build/libs/*.jar
            paper/build/libs/*.jar
            bukkit/build/libs/*.jar
            neoforge/build/libs/*.jar
            forge/build/libs/*.jar
            velocity/build/libs/*.jar
            bungeecord/build/libs/*.jar
            !**/*-sources.jar
            !**/*-javadoc.jar
            !**/*-dev.jar
